订阅kafka 消息目前做了两个版本：
1. 订阅一个小时的数据
2. 订阅7天的数据

算法：
输入一个时间
根据时间去找kafka offset（如果找不到offset，退出）找offset函数的原理是找到比指定时间戳大的offset，所以输入的时间很小也能找到offset
退出条件是到达结束时间或者找到offset的末尾，避免一直订阅程序不退出

创建多线程订阅，每个线程负责一个partition，判断会方便很多